# ── Banco de dados ────────────────────────────────────────────────────────
POSTGRES_PASSWORD=construprice123

# ── Segurança ─────────────────────────────────────────────────────────────
# Gere uma chave segura: python3 -c "import secrets; print(secrets.token_hex(32))"
CREDENTIALS_ENCRYPTION_KEY=troque-isso-em-producao

# ── Frontend ──────────────────────────────────────────────────────────────
# URL pública do backend (use o domínio real em produção)
NEXT_PUBLIC_API_URL=http://localhost:8000

# URL pública do frontend (para configurar CORS no backend)
FRONTEND_URL=http://localhost:3000

# ── Credenciais dos fornecedores (opcional) ───────────────────────────────
SUPPLIER_ESTOQUE_MEGALESTE_USERNAME=
SUPPLIER_ESTOQUE_MEGALESTE_PASSWORD=
SUPPLIER_COFEMA_MATERIAIS_USERNAME=
SUPPLIER_COFEMA_MATERIAIS_PASSWORD=
SUPPLIER_ESTOQUE_ATACADISTA_USERNAME=
SUPPLIER_ESTOQUE_ATACADISTA_PASSWORD=

# ── Logging ───────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
